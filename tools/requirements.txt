# PASR Python Tools — pip install -r tools/requirements.txt

# Core data
pandas>=2.0
numpy>=1.24

# Machine learning (retrain_ensemble.py)
scikit-learn>=1.3

# Visualization (retrain_ensemble.py + walkforward_harness.py)
matplotlib>=3.7

# Transformer / ONNX pipeline (export_onnx.py, train_transformer.py planned)
onnx>=1.16
# onnxruntime>=1.16
# torch>=2.0
